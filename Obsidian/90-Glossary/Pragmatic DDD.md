# Pragmatic DDD

## 是什么
- 指“务实版 DDD”。
- 不是把 DDD 的所有战术模式铺满全项目，而是只在真正值得建模的核心域使用。

## 在本项目里怎么用
- 会话、路由、知识发布、素材治理这些高业务约束域采用 Pragmatic DDD。

## 生产里要注意
- 目标是守住规则，不是追求模式完整度。
- 如果某个服务主要是接入适配或流程编排，就不该强上重领域模型。

## 面试怎么说
- “Pragmatic DDD 的核心是按收益使用领域建模，而不是为了架构名词把所有服务都做重。”
