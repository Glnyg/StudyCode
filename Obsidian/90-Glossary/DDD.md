# DDD

## 是什么
- Domain-Driven Design，中文通常叫“领域驱动设计”。
- 核心思想是让代码结构围绕业务领域和业务规则，而不是只围绕数据库表。

## 在本项目里怎么用
- 主要用于会话、路由、知识发布、素材治理这些强业务约束域。

## 生产里要注意
- DDD 不是所有服务都要强上。
- 如果服务只是适配器、投影器、报表器，硬上 DDD 往往只会变复杂。

## 面试怎么说
- “DDD 最适合业务规则复杂、状态迁移多、错误代价高的核心域，不适合无差别覆盖整个系统。”
