# .NET 10 LTS

## 是什么
- `.NET 10` 是微软的长期支持版本。
- `LTS` 表示长期支持，适合生产系统。

## 在本项目里怎么用
- 作为所有新服务的统一运行时基线。
- 配合 [[ASP.NET Core]]、[[gRPC]]、[[SignalR]] 搭建后端服务。

## 生产里要注意
- 统一 SDK、运行时、CI 镜像版本。
- 不要混用多个主版本导致环境漂移。

## 面试怎么说
- “新系统我会优先选当前可用的 LTS 版本，减少短周期升级成本。”
